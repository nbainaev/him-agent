checkpoint_path: "configs/agent/bio/encoder/vae/checkpoints/cat5_50_beta1_0.ckpt"
use_camera: false
model_params:
  in_channels: 3
  latent_dim: 5
  categorical_dim: 50
  temperature: 0.5
  anneal_rate: 0.00003
  anneal_interval: 100
  alpha: 1.0
  beta: 1.0  # reward regularization weight

